<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!--
  core-site.xml — Hadoop core configuration
  fs.defaultFS points to NameNode inside Docker network
-->
<configuration>

  <!-- Default filesystem — NameNode RPC endpoint -->
  <property>
    <name>fs.defaultFS</name>
    <value>hdfs://namenode:9000</value>
    <description>NameNode URI. Use hdfs://localhost:9000 for single-node local mode.</description>
  </property>

  <!-- Temporary directory for Hadoop processes -->
  <property>
    <name>hadoop.tmp.dir</name>
    <value>/hadoop/tmp</value>
    <description>Base directory for temporary files.</description>
  </property>

  <!-- HTTP static user for Web UIs (no auth in dev) -->
  <property>
    <name>hadoop.http.staticuser.user</name>
    <value>root</value>
  </property>

  <!-- Allow all users to proxy (needed for Spark submit) -->
  <property>
    <name>hadoop.proxyuser.root.hosts</name>
    <value>*</value>
  </property>

  <property>
    <name>hadoop.proxyuser.root.groups</name>
    <value>*</value>
  </property>

</configuration>
